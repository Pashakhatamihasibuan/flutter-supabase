import 'package:flutter/material.dart';

class GetstartedViews extends StatelessWidget {
  const GetstartedViews({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("GetstartedViews"),
      ),
    );
  }
}
